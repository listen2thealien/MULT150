using System.Threading;
using UnityEngine;
public class Evens : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
       // count even numbers from 22 to 100
    for (int i = 22; i <= 100; i += 2)

 Debug.Log(i);
 }
     // Update is called once per frame
    void Update()
    {
        
    }
}